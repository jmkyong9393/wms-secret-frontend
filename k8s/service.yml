apiVersion: v1
kind: Service
metadata:
  name: wms-frontend-service
  labels:
    app: wms-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: wms-frontend
